# Independent verification — FAIL

Date: 2026-08-28
Work order: `receipt-statement-linker-verify-3`
Candidate: `a2dfe114ee23396253040a5ea7144527335d4254` (`docs: record deployed repair verification`)
Live URL: <https://receipt-statement-linker.sociobot.in>

## Decision

**FAIL — do not release at the required URL.** The candidate builds cleanly and its locally packaged extension works end to end. The current deployment serves the candidate's HTML, JS, CSS, legal pages, and service worker byte-for-byte, but omits the extension archive advertised by every install CTA. `GET` and `HEAD` on the required archive URL return a plain `404 text/html`, so a user cannot download or install the product.

## Release-blocking defect

| Severity | Fresh evidence | Required remediation |
| --- | --- | --- |
| **P0 / release blocker** | `https://receipt-statement-linker.sociobot.in/downloads/receipt-statement-linker-chrome.zip` returned `404`, `content-type: text/html`, and a 2,400-byte "404: Not Found" body on 2026-08-28. In fresh Chromium, clicking **Download for Chromium** raised a download event whose `failure()` was `canceled`; it yielded no bytes. Direct navigation returned the same 404. The exact local archive exists, is 23,611 bytes, SHA-256 `ca8090896b4b9c3375f1e661ce0efc37903f5888788d5913252681652a81e332`, and `unzip -t` passes. | Deploy the exact `dist/site/` generated by `npm run build`, including `downloads/receipt-statement-linker-chrome.zip`. Confirm live `200`, `Content-Type: application/zip`, attachment disposition, the matching SHA-256, and a Chromium download that is not canceled before release. |

The candidate's `staticwebapp.config.json` correctly excludes `/downloads/*` from navigation fallback and declares archive headers. This evidence is therefore an artifact/deployment omission, not an incorrect CTA or a stale public-site build.

## Clean-checkout local verification

The repository was clean at the requested SHA before installation. `npm ci` installed the locked 178 packages and `npm audit --omit=dev --audit-level=high` reported zero vulnerabilities.

| Check | Result |
| --- | --- |
| `npm test` | PASS — 9/9 Vitest tests. |
| `npm run check` | PASS — `tsc --noEmit`. No lint script is defined. |
| `npm run build` | PASS — exact production command created `dist/site/`, `dist/extension/chrome-mv3/`, and the Chrome ZIP. |
| `npm run test:e2e` | PASS — 8/8 Playwright/axe tests, including download against the local artifact, first controlled offline reload, desktop keyboard entry, and 390px layout. |
| `npm run test:extension` | PASS — built MV3 smoke: capture → CSV import → explicit approval → CSV export, desktop and 390px, no console errors or serious/critical axe findings. |
| Archive and budgets | PASS — ZIP integrity test passed; unpacked extension is 49,339 bytes; static initial JS is 2,666 bytes and static CSS is 10,526 bytes (both within the 200 KB / 50 KB budgets). |

### Independent extension workflow

In a fresh Chromium MV3 profile loaded from `dist/extension/chrome-mv3/`, I separately exercised:

- Empty capture and a representative receipt: `North Star Market`, `48.20`, `2026-05-13`, and an HTTPS receipt URL; then normal CSV import, explicit approval, enriched CSV export, and attachment-manifest export. The exported CSV contained the receipt URL and `approved`; the manifest contained one approved `North Star Market` link.
- Invalid/recovery paths: amount `0` is rejected, focuses the amount field, and can be corrected; an invalid receipt URL gets native validation focus and can be corrected; malformed quoted CSV, CSV with no readable date/amount, and a 3 MB+ CSV all show actionable messages and retain the existing two-row, one-approval workspace.
- Destructive recovery: canceling **Clear statement** leaves the workspace intact; confirming it removes only the transient statement and approvals.
- Free boundary: at 25 saved receipts the popup disables capture and explains the limit.
- Desktop and 390×844 workbench layouts: no horizontal overflow in normal and 25-receipt boundary states; Tab exposes a visible focus target. Reduced-motion styling resolves transitions to `0.01ms`.
- Fresh independent axe scans of the popup and workbench reported zero serious/critical findings. Normal capture/import activity made zero external requests and produced zero console errors.

## Live deployment, browser, privacy, and response-policy evidence

The following public resources byte-match the fresh candidate build:

| Resource | SHA-256 |
| --- | --- |
| `/` | `b9b6f12ec20e0b3b3ada6f950c7583930752d8d94e7f98f53c92fb89c4e5fe15` |
| `/privacy/` | `99b1adcabfa759b4fd5c5a2d28581d9bf8cab5ad8065339b0114676ae8091ef1` |
| `/terms/` | `e21b00971e8ad772c692ce0b8b2b998b6e4db93951d9eb997d8a881fcdcc3a6e` |
| `/sw.js` | `90ba7a21b9b7e26cd9ca682c150a89837b7d5cd6886a2c2622fb348cb0d629dc` |
| `/assets/main-Ooh6Hzfm.js` | `042eb099c61a5a2730fe4529aa59aa955425fbc49f67b95407bb9313521e00d3` |

- Fresh Chromium desktop checks on `/`, `/privacy/`, and `/terms/` found one `h1`, one `main`, correct titles and `lang=en`, zero console/page errors, zero serious/critical axe findings, and no automatic outbound origin beyond the product origin.
- At 390×844: no horizontal overflow; first Tab focuses **Skip to main content** with a 3px outline; reduced motion is honored. The static service worker completed a first controlled offline reload with the landing heading present and no console errors. This is a browser-extension product rather than a PWA; its static-site worker's release-version and old-cache-cleanup behavior is additionally covered by the passing unit suite.
- Live headers for HTML and `sw.js` include `Cache-Control: no-cache, no-store, must-revalidate`; hashed assets have `public, max-age=31536000, immutable`. HSTS, `X-Content-Type-Options: nosniff`, strict referrer policy, CSP with `frame-ancestors 'none'`, `X-Frame-Options: DENY`, and restrictive permissions policy are present. The absent ZIP naturally cannot satisfy its configured archive policy.
- Static inspection and network capture found no analytics, trackers, remote fonts, or third-party scripts. The built manifest has only `storage`, `downloads`, and `activeTab`, with no host permissions or content scripts. Receipt and statement state uses `chrome.storage.local`; normal capture/import/export made no network request. The only product API endpoint is the documented Sociobot license verification after an explicit license action or stored-license recheck.
- Lighthouse 13 mobile on the live landing page: **100 Performance / 100 Accessibility / 100 Best Practices / 100 SEO**; FCP 1.0 s, LCP 1.1 s, Speed Index 1.2 s, TBT 0 ms, CLS 0. Lighthouse emitted a post-report `TARGET_CRASHED` warning while collecting screenshot/BFCache artifacts, but wrote a complete scored report; the independent Playwright session had no browser errors.

## Retest gate

After deployment publishes the archive, repeat the live archive header/hash/download checks, install the downloaded archive into a fresh Chromium profile, and rerun the normal capture → import → explicit approval → CSV/manifest export flow. All other independently tested candidate behavior passed.
